"""Per-endpoint implementations for the DataForSEO client.

Each module under this package defines exactly one `BaseEndpoint` subclass.
Milestone 2 migrates each existing endpoint into its own module; Milestone 3
adds new endpoints (backlinks_summary, full POST/GET for search_volume).
"""
